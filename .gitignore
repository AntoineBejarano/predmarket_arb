# Data (too large for git)
data/raw/
data/zips/
data/features/

# Models (small enough — keep in git for Railway)
# models/saved/ ← DO NOT ignore

# Jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
.venv/

# Logs (Railway generates these)
logs/

# Local exploration outputs (PNGs, scratch); strategy evidence lives under strategies/
reports/

# Strategy lab — keep LEARNINGS/RUNBOOK + small JSON/MD in git; ignore heavy outputs
strategies/**/data/
strategies/**/scratch/
strategies/**/experiments/**/*.png
strategies/**/experiments/**/*.parquet
strategies/**/experiments/**/*.zip

# Local agent / IDE artefacts
.claude/

# Secrets
.env
